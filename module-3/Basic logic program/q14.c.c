//14.find ascii value of given number
#include<stdio.h>
int main()
{
	int num;
	printf("/nEnter the num =");
	scanf("%d",&num);
	
	if(num==0)
	{
		printf("32 is the ascii value of the number % d.",num);
	}
	if(num==1)
	{
		printf("33 is the ascii value of the number % d.",num);
	}
	if(num==2)
	{
		printf("34 is the ascii value of the number % d.",num);
	}
	if(num==3)
	{
		printf("35 is the ascii value of the number % d.",num);
	}
	if(num==4)
	{
		printf("36 is the ascii value of the number % d.",num);
	}
	if(num==5)
	{
		printf("37 is the ascii value of the number % d.",num);
	}
	if(num==6)
	{
		printf("38 is the ascii value of the number % d.",num);
	}
	if(num==7)
	{
		printf("39 is the ascii value of the number % d.",num);
	}
	if(num==8)
	{
		printf("40 is the ascii value of the number % d.",num);
	}
	if(num==9)
	{
		printf("41 is the ascii value of the number % d.",num);
	}
	return 0;
}
